WORLDVOICECM GitHub Pages FIXED version.
The main website image is embedded directly inside index.html, so there are no image paths to break.
Upload index.html to the repository root and replace the old index.html.
